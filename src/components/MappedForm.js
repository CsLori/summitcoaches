       // const { user, userArray } = this.state;

   
   {/* <div>
          {userArray.map(data => (
            <div key={data}>
              <label htmlFor={data}>{data}</label>
               {console.log(data !== 'password'  && data !== 'email' ? 'text' : data )} 
              <input
                type={data !== 'password'  && data !== 'email' ? 'text' : data }
                value={user[data]}
                name={data}
                onChange={this.handleChange}
              />
            </div>
          ))}
        </div>  */}